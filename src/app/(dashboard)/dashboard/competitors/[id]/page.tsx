import { redirect, notFound } from "next/navigation"
import Link from "next/link"
import { auth } from "@/auth"
import { getDb } from "@/lib/db"
import { organizations, organizationMembers, trackedCompetitors, trackedProducts } from "@/lib/db/schema"
import { eq, and, desc, count } from "drizzle-orm"
import { ArrowLeft, ExternalLink, TrendingDown, TrendingUp, Package, RefreshCw } from "lucide-react"
import { AddProductModal } from "@/components/dashboard/add-product-modal"
import { DeleteProductButton } from "@/components/dashboard/delete-product-button"
import { SetPriceButton } from "@/components/dashboard/set-price-button"
import { getLocale } from "@/lib/i18n/locale"
import { withUnlimitedAccess } from "@/lib/admin"

const DICT={fr:{active:"Actif",inactive:"Inactif",trackedProducts:"Produits suivis",priceDrops:"Baisses de prix",priceRises:"Hausses de prix",outOfStock:"En rupture",products:(n:number)=>`Produits (${n})`,lastScan:(date:string)=>`Dernier scan ${date}`,noProduct:"Aucun produit ajouté",noProductDesc:"Ajoutez l'URL exacte d'un produit pour surveiller son prix.",product:"Produit",currentPrice:"Prix actuel",variation:"Variation",stock:"Stock",inStock:"En stock",outOfStockShort:"Rupture",sku:"SKU"},en:{active:"Active",inactive:"Inactive",trackedProducts:"Tracked products",priceDrops:"Price drops",priceRises:"Price rises",outOfStock:"Out of stock",products:(n:number)=>`Products (${n})`,lastScan:(date:string)=>`Last scan ${date}`,noProduct:"No product added",noProductDesc:"Add the exact product URL to monitor its price.",product:"Product",currentPrice:"Current price",variation:"Change",stock:"Stock",inStock:"In stock",outOfStockShort:"Out of stock",sku:"SKU"}}
export default async function CompetitorDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const session=await auth();if(!session?.user?.id)redirect("/auth/login");const locale=await getLocale();const t=DICT[locale];const db=getDb();const [membership]=await db.select({org:organizations}).from(organizationMembers).innerJoin(organizations,eq(organizationMembers.organizationId,organizations.id)).where(eq(organizationMembers.userId,session.user.id)).limit(1);if(!membership)redirect("/onboarding");const org=withUnlimitedAccess(membership.org,session.user.email);const [competitor]=await db.select().from(trackedCompetitors).where(and(eq(trackedCompetitors.id,id),eq(trackedCompetitors.organizationId,org.id))).limit(1);if(!competitor)notFound();const [products,[{totalProducts}]]=await Promise.all([db.select().from(trackedProducts).where(and(eq(trackedProducts.competitorId,competitor.id),eq(trackedProducts.isActive,true))).orderBy(desc(trackedProducts.createdAt)).limit(100),db.select({totalProducts:count()}).from(trackedProducts).where(and(eq(trackedProducts.organizationId,org.id),eq(trackedProducts.isActive,true)))]);const priceChanges=products.filter(p=>p.priceChangePercent!=null);const drops=priceChanges.filter(p=>(p.priceChangePercent??0)<0).length;const rises=priceChanges.filter(p=>(p.priceChangePercent??0)>0).length;const outOfStock=products.filter(p=>p.isInStock===false).length;return <div className="p-6 space-y-6 bg-[#08090C] min-h-full"><div className="flex items-center gap-3"><Link href="/dashboard/competitors" className="text-gray-500 hover:text-white"><ArrowLeft className="h-4 w-4"/></Link><div className="flex-1"><div className="flex items-center gap-3"><h1 className="text-xl font-bold text-white">{competitor.name}</h1><span className="text-xs px-2 py-0.5 rounded-full bg-white/10 text-gray-300">{competitor.platform}</span></div><a href={`https://${competitor.domain}`} target="_blank" rel="noopener" className="text-sm text-gray-500 hover:text-gray-300 flex items-center gap-1 mt-0.5 w-fit"><ExternalLink className="h-3 w-3"/>{competitor.domain}</a></div><AddProductModal productLimit={org.productLimit} currentCount={totalProducts} locale={locale}/><div className={`flex items-center gap-1.5 text-xs ${competitor.isActive?"text-[#8B5CF6]":"text-gray-500"}`}><div className={`h-2 w-2 rounded-full ${competitor.isActive?"bg-[#8B5CF6]":"bg-gray-500"}`}/>{competitor.isActive?t.active:t.inactive}</div></div><div className="grid grid-cols-2 sm:grid-cols-4 gap-4">{[{label:t.trackedProducts,value:products.length,icon:Package},{label:t.priceDrops,value:drops,icon:TrendingDown},{label:t.priceRises,value:rises,icon:TrendingUp},{label:t.outOfStock,value:outOfStock,icon:Package}].map(({label,value,icon:Icon})=><div key={label} className="bg-white/5 border border-white/8 rounded-xl p-4"><div className="flex items-center justify-between mb-2"><p className="text-xs text-gray-500">{label}</p><Icon className="h-4 w-4 text-[#A78BFA]"/></div><p className="text-2xl font-black text-white">{value}</p></div>)}</div><div className="bg-white/5 border border-white/8 rounded-2xl overflow-hidden"><div className="flex items-center justify-between px-5 py-4 border-b border-white/8"><h2 className="font-semibold text-white text-sm">{t.products(products.length)}</h2>{competitor.lastScrapedAt&&<span className="text-xs text-gray-500 flex items-center gap-1.5"><RefreshCw className="h-3 w-3"/>{t.lastScan(new Date(competitor.lastScrapedAt).toLocaleString(locale==="en"?"en-US":"fr-FR",{dateStyle:"short",timeStyle:"short"}))}</span>}</div>{products.length===0?<div className="p-12 text-center"><div className="h-14 w-14 rounded-2xl bg-[#8B5CF6]/10 border border-[#8B5CF6]/20 flex items-center justify-center mx-auto mb-4"><Package className="h-6 w-6 text-[#A78BFA]"/></div><p className="text-sm font-medium text-white mb-1">{t.noProduct}</p><p className="text-xs text-gray-400 mb-4">{t.noProductDesc}</p><AddProductModal productLimit={org.productLimit} currentCount={totalProducts} locale={locale}/></div>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b border-white/8 text-xs text-gray-500"><th className="text-left px-5 py-3 font-medium">{t.product}</th><th className="text-right px-4 py-3 font-medium">{t.currentPrice}</th><th className="text-right px-4 py-3 font-medium">{t.variation}</th><th className="text-center px-4 py-3 font-medium">{t.stock}</th><th className="px-4 py-3"/></tr></thead><tbody>{products.map(product=><tr key={product.id} className="border-b border-white/5 hover:bg-white/3"><td className="px-5 py-3.5"><a href={product.url} target="_blank" rel="noopener" className="text-white hover:text-[#A78BFA] truncate max-w-xs block font-medium">{product.name||product.url}</a>{product.sku&&<p className="text-xs text-gray-500 mt-0.5">{t.sku}: {product.sku}</p>}</td><td className="px-4 py-3.5 text-right">{product.currentPrice!=null?<><p className="font-bold text-white">{product.currentPrice.toFixed(2)} {product.currency??"€"}</p>{product.previousPrice!=null&&<p className="text-xs text-gray-500 line-through">{product.previousPrice.toFixed(2)} {product.currency??"€"}</p>}</>:<SetPriceButton productId={product.id} locale={locale}/>}</td><td className="px-4 py-3.5 text-right">{product.priceChangePercent!=null?<span className={`text-xs font-bold px-2 py-0.5 rounded-lg ${product.priceChangePercent<0?"bg-emerald-500/15 text-emerald-400":"bg-red-500/15 text-red-400"}`}>{Math.abs(product.priceChangePercent).toFixed(1)}%</span>:<span className="text-gray-600">—</span>}</td><td className="px-4 py-3.5 text-center">{product.isInStock===null?<span className="text-gray-600 text-xs">—</span>:product.isInStock?<span className="text-xs text-emerald-400 bg-emerald-500/10 px-2 py-0.5 rounded-full">{t.inStock}</span>:<span className="text-xs text-orange-400 bg-orange-400/10 px-2 py-0.5 rounded-full">{t.outOfStockShort}</span>}</td><td className="px-4 py-3.5"><DeleteProductButton productId={product.id} locale={locale}/></td></tr>)}</tbody></table></div>}</div></div>}
